#include "monty.h"

/**
 * f_queue - prints the top
 * @heads: stack head
 * @count: line_number
 * Return: no return
 */
void f_queue(stack_t **heads, unsigned int count)
{
	(void)heads;
	(void)count;
	Bus.line = 1;
}

